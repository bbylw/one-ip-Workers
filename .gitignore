# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.wrangler
.dev.vars
.dev.vars.*
worker-configuration.d.ts
.worker-test
# SPA files embedded into the Worker bundle by scripts/build-worker-assets.mjs
public/worker/assets.generated.js

# Layout/colour review screenshots written by scripts/verify-ui.mjs. Regenerate
# with that script instead of committing: the captures expose the location, ASN
# and browser fingerprint of the machine that ran them, even though the IP
# octets are masked. README images stay hand-redacted under docs/screenshots.
docs/screenshots/ui-review/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Deployment secrets (never commit)
.secrets.*.env
